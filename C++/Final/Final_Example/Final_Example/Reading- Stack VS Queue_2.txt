

- 스택

후입선출 LIFO 

 삽입과 삭제는 top이라 부르는 곳에서 이루어진다.
 삽입은 push, 삭제는 pop
 스택에선 1개의 포인터만이 리스트에 접근하는 데에 이용될 수 있다.
이는 top이라고 불리며, 항상 마지막을 가리키고 있다.

- 큐

선입선출 FIFO

 삽입과 삭제는 각각 양 끝, front와 back에서 이루어진다.
 삽입은 push(맨 뒤), 삭제는 pop(맨 앞)
 큐에선 2개의 포인터를 이용해서 리스트에 접근 가능하다. 각각 맨 앞과 맨 뒤를 가리키는 포인터.

 